/**
 * The application worker, named where `gesso-vite-plugin` looks for it.
 *
 * The wiring itself is `app/AppWorker.ts`, beside the layers it wires,
 * so that everything about the sheet lives in one directory and this
 * file is only an address.
 */
import './app/AppWorker';
